import { Card } from "../../../ui/Card";

export function ListPage() {
  return (
    <Card title="Acadêmico · Lista" subtitle="Tabela/lista (placeholder).">
      <div className="panel-2 rounded-2xl p-4 text-sm text-[rgb(var(--muted))]">
        Aqui entra uma lista com filtros, paginação e ações.
      </div>
    </Card>
  );
}
